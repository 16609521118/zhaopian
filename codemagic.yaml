# ============================================================
# PhotoViewer 的 Codemagic 云构建配置
# 项目：iOS 原生 App（Swift 5 + UIKit + WKWebView），无第三方依赖
# 用途：在 Codemagic 云上自动构建出可用于侧载/自签安装的 IPA
#
# 使用步骤：
#   1. 把整个项目（含本文件）推送到 GitHub / GitLab / Bitbucket 仓库
#   2. 在 Codemagic 控制台点 "Add application" 关联该仓库
#   3. 推送分支 / 创建 PR 会自动构建，也可在控制台手动触发
#   4. 构建完成后，在本次构建页面的 Artifacts 中下载 IPA
#
# 注意：本仓库已包含共享 Scheme（PhotoViewer.xcodeproj/xcshareddata/），
#       不要删除，否则云端 xcodebuild -scheme 会找不到 Scheme。
# ============================================================

workflows:
  # ----------------------------------------------------------
  # 工作流一：构建"不签名"IPA（推荐，无需 Apple 开发者证书）
  # 下载后用 AltStore / Sideloadly / TrollStore 等工具自签安装
  # ----------------------------------------------------------
  photo-viewer-unsigned:
    name: PhotoViewer - 构建不签名 IPA（侧载用）
    instance_type: mac_mini_m2
    max_build_duration: 120

    environment:
      xcode: latest        # 使用最新版 Xcode；如需固定版本，改为如 16.4
      vars:
        XCODE_PROJECT: PhotoViewer.xcodeproj
        XCODE_SCHEME: PhotoViewer
        XCODE_CONFIGURATION: Release
        BUNDLE_ID: com.photoviewer.app   # 与 Xcode 工程中的 Bundle Identifier 保持一致

    # 触发方式：推送任意分支 / 创建 PR 时自动构建（也可在 Codemagic 界面手动触发）
    triggering:
      events:
        - push
        - pull_request
      branch_patterns:
        - pattern: '*'
          include: true
          source: true

    scripts:
      - name: 构建 App（关闭代码签名）
        script: |
          set -e
          xcodebuild \
            -project "$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME" \
            -configuration "$XCODE_CONFIGURATION" \
            -sdk iphoneos \
            -derivedDataPath build/ios \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            CURRENT_PROJECT_VERSION="$BUILD_NUMBER" \
            clean build

      - name: 打包不签名 IPA 与 .app 压缩包
        script: |
          set -e
          APP_PATH="build/ios/Build/Products/Release-iphoneos/PhotoViewer.app"
          # 1) 打包成 IPA（供 AltStore / Sideloadly / TrollStore 等工具安装）
          mkdir -p build/ios/ipa/Payload
          cp -R "$APP_PATH" build/ios/ipa/Payload/
          (cd build/ios/ipa && zip -qry "PhotoViewer-unsigned-${BUILD_NUMBER}.ipa" Payload)
          # 2) 同时打包 .app 压缩包（部分工具可直接使用）
          (cd build/ios/Build/Products/Release-iphoneos && zip -qry "$CM_BUILD_DIR/build/ios/PhotoViewer-app-${BUILD_NUMBER}.zip" PhotoViewer.app)

    artifacts:
      - build/ios/ipa/*.ipa
      - build/ios/*.zip
      - build/ios/Build/Products/Release-iphoneos/*.dSYM

  # ----------------------------------------------------------
  # 工作流二（默认注释）：签名构建 Ad Hoc IPA
  # 需要：Apple Developer 账号 + 在 Codemagic 上传签名证书
  # 启用步骤：
  #   1. 取消下方所有 "#" 注释
  #   2. 在 Codemagic 项目设置 > Signing 中上传 P12 证书与描述文件
  #      （或在项目环境变量中配置 CM_CERTIFICATE / CM_CERTIFICATE_PASSWORD）
  #   3. 重新触发构建
  # ----------------------------------------------------------
  # photo-viewer-signed:
  #   name: PhotoViewer - 签名构建 Ad Hoc IPA（需开发者证书）
  #   instance_type: mac_mini_m2
  #   max_build_duration: 120
  #
  #   environment:
  #     xcode: latest
  #     ios_signing:
  #       distribution_type: ad_hoc
  #       bundle_identifier: com.photoviewer.app
  #     vars:
  #       XCODE_PROJECT: PhotoViewer.xcodeproj
  #       XCODE_SCHEME: PhotoViewer
  #
  #   triggering:
  #     events:
  #       - push
  #     branch_patterns:
  #       - pattern: main
  #         include: true
  #         source: true
  #
  #   scripts:
  #     - name: 归档并导出签名 IPA
  #       script: |
  #         set -e
  #         xcodebuild -project "$XCODE_PROJECT" -scheme "$XCODE_SCHEME" \
  #           -configuration Release -sdk iphoneos \
  #           -derivedDataPath build/ios \
  #           -archivePath build/ios/PhotoViewer.xcarchive archive
  #         # 生成 ad-hoc 导出配置
  #         cat > build/ios/ExportOptions.plist <<'EOF'
  #         <?xml version="1.0" encoding="UTF-8"?>
  #         <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
  #         <plist version="1.0">
  #         <dict>
  #           <key>method</key>
  #           <string>ad-hoc</string>
  #           <key>signingStyle</key>
  #           <string>automatic</string>
  #         </dict>
  #         </plist>
  #         EOF
  #         xcodebuild -exportArchive \
  #           -archivePath build/ios/PhotoViewer.xcarchive \
  #           -exportOptionsPlist build/ios/ExportOptions.plist \
  #           -exportPath build/ios/ipa
  #
  #   artifacts:
  #     - build/ios/ipa/*.ipa
  #
  #   publishing:
  #     email:
  #       recipients:
  #         - your@email.com
  #       notify:
  #         success: true
  #         failure: true
